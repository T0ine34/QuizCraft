
/**
 * Enum for HTTP status codes
 * @readonly
 * @enum {number}
 */
export const enum HTTPCodes {
    OK = 200,
    Created = 201,
    BadRequest = 400,
    Unauthorized = 401,
    Forbidden = 403,
    NotFound = 404,
    InternalServerError = 500
}